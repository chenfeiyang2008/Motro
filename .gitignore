node_modules/
.pnpm-store/
.next/
dist/
coverage/
playwright-report/
test-results/
# 共享登录会话状态（Playwright storageState，含临时 cookies，不入库）。
tests/e2e/.auth/

# TypeScript build artifacts
*.tsbuildinfo
.eslintcache

.env
.env.*
!.env.example

.DS_Store
*.log

# Runtime data and secrets
data/
backups/
secrets/

# Local import file storage (server-only writable import root)
.local-import-files/

# Local UI skill runtime state; installed skill definitions remain tracked.
.impeccable/

# Local scratch / ad-hoc working dirs — never committed (debug scripts, agent notes, demos).
.scratch/
.vscode/

# Installed UI agent skills (definitions are fetched at runtime, not source).
.agents/skills/

# Local Claude worktrees created by the harness.
.claude/worktrees/
# Local Claude harness config (per-machine; not source, not secret).
.claude/settings.json
