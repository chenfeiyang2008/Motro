# 待决策登记

本文只登记会改变范围、供应商、成本、安全或部署方式的决定。能安全延后的决定不阻塞当前开发。

## 已决定

### 1. Ticket 03 的本地提交

- **决定**：全部最终门禁通过后已按精确文件清单创建提交 `0c04926`，未推送。
- **结果**：工单 04 已解锁。

## 现在需要确认

### 2. 已安装 UI/工具技能如何入库

- **现状**：`.agents/skills/` 与 `skills-lock.json` 有工具安装器产生的未提交改动。
- **建议**：暂不混入 Ticket 03；先按 [`../ui/skill-security.md`](../ui/skill-security.md) 审计，之后创建独立 tooling commit。
- **需要你做**：当前无需立即决定；提交前我会给出保留/删除清单。

## 在对应工单前决定

### 3. Wiktionary 访问与许可策略（阶段 6 / 工单 05 前）

- 需要确认访问接口、User-Agent、限速、缓存、修订标识、许可与归属展示。
- **建议默认**：先用官方 MediaWiki/Wiktionary 一手文档做适配器研究；正常测试只用仓库 fixture；真实 smoke 单独门控。
- **需要你做**：届时批准研究结论，不需要现在提供账号。

### 4. DeepSeek 应用供应商配置（阶段 6 / 工单 06 前）

- Claude Code 的第三方模型配置不等于 Motro 应用的 DeepSeek 配置。
- 需要单独确定 API endpoint、模型、预算、超时和服务器端密钥管理。
- **建议默认**：OpenAI-compatible 窄 adapter、严格 JSON schema、fake provider 全覆盖；真实调用默认关闭。
- **需要你做**：届时选择供应商/模型并以本地 secret 方式提供 key；不要把 key 发进聊天或提交到仓库。

### 5. 正式图标体系（阶段 8 / UI 系统收尾前）

- 当前仓库没有锁定的产品图标组件，过去因此保留了纯文字导航。
- **建议默认**：阶段 8 单独评估并精确锁定 Lucide 或 Motro 自有图标封装；不在业务票临时安装。
- **需要你做**：届时在两套视觉样例中选择方向。

### 6. 家庭服务器实机参数（阶段 9 前）

- 需要真实 OS/版本、CPU/内存/磁盘、Tailscale 域名、数据目录、NAS/备份目标、恢复窗口。
- **建议默认**：4 GB 主机预算、容器资源限额、加密 30 天备份、空机恢复演练。
- **需要你做**：阶段 9 开始前提供这些非秘密参数；备份口令只在本机 secret 管理中设置。

## 已延后，不阻塞当前路线

- 会员/付费系统：暂不做；用户已确认延后。
- 语法学习：可作为独立产品阶段研究，不混入当前词汇学习与内容管线。
- XP、等级、任务、徽章、排行榜与周挑战：阶段 7 已有规划。
- 学习仪表盘高级指标与最终视觉精修：阶段 7 提供事实，阶段 8 完成呈现；CEFR 只能作为带来源的词汇覆盖参考，不能宣称综合语言能力。
- PWA、离线同步、Android、iOS、macOS：Web 稳定后再发现，不提前写客户端工单。
