# 项目当前状态

更新时间：2026-08-13

本文记录 Motro 的实施状态与当前执行门。它不替代产品、领域或架构规范：

- 产品边界以 [`PRODUCT.md`](../../PRODUCT.md) 为准；
- 领域词汇以 [`CONTEXT.md`](../../CONTEXT.md) 为准；
- 架构决定以 [`docs/adr/`](../adr/) 为准；
- 视觉与交互以 [`DESIGN.md`](../../DESIGN.md) 和 [`docs/ui/`](../ui/) 为准；
- 每张工单的实现范围以对应 `.scratch/<phase>/issues/` 文件为准。

## 阶段总览

| 阶段 | 状态 | 已完成事实 | 当前出口 |
| --- | --- | --- | --- |
| 1. 规格基线 | 已完成 | 产品、领域、架构、API、测试与 UI 基线已建立 | 后续变更继续通过 ADR/规范演进 |
| 2. 设计验证 | 基线已确认 | 学习者关键页面原型、橙色 Glass 方向、动效与材料规范已建立 | 响应式与视觉回归在阶段 8 统一收尾 |
| 3. 平台基础 | 已完成 | monorepo、CI、Compose、PostgreSQL、Nest、Next、OpenAPI、配置与认证闭环 | 提交 `12b67fb` |
| 4. 手工内容闭环 | 已完成 | 词条、课程草稿、校验、不可变发布、目录、报名与主课程 | 提交 `cfc7dad` |
| 5. 学习核心 | 已完成 | 双向学习卡、FSRS v6、每日计划、可恢复会话、评分、进度与学习 Web | 提交 `59021a1` |
| 6. 内容管线 | 进行中 | 工单 01–03 已提交并通过验收 | 工单 04 已解锁，等待正式 To-Agent 实施 |
| 7. 激励与运营 | 已规划，未开始 | 11 张有序工单已定义 | 等待阶段 6 完成 |
| 8. 质量、设计与容量 | 已规划，未开始 | 9 张收尾工单已定义 | 等待阶段 7 完成；集中完成最终 UI 精修 |
| 9. 家庭服务器发布 | 已规划，未开始 | 9 张发布工单已定义 | 等待阶段 8 完成及真实服务器参数确认 |
| 10. 原生客户端发现 | 延后 | 仅保留发现阶段占位 | Web 稳定后单独立项，不继承 Web UI 实现 |

## 阶段 6 进度

| 工单 | 状态 | 证据/备注 |
| --- | --- | --- |
| 01 原始文件与导入批次 | 已提交 | `6ca80d8` |
| 02 四格式解析、映射与校验 | 已提交 | `5e6aaaf` |
| 03 有效行提交与错误报告 | 已提交 | `0c04926`；43/43 测试文件、508/508 测试和隔离 E2E 24/24 通过 |
| 04 Worker 操作与任务状态基础 | `ready-for-agent` | 下一张业务工单；禁止混入供应商 HTTP adapter |
| 05 Wiktionary 补全与来源追溯 | 阻塞 | 依赖工单 04 |
| 06 DeepSeek 中文草稿 | 阻塞 | 依赖工单 05；需要单独应用密钥与供应商配置 |
| 07 管理员审核与词条落地 | 阻塞 | 依赖补全草稿事实 |
| 08 阶段验收与失败恢复 | 阻塞 | 不增加功能，只做闭环证据 |

## Ticket 03 收口证据

下列门禁均已满足：

1. Ticket 03 的静态门禁、全量测试和独立 PostgreSQL 集成全绿；
2. `pnpm run e2e:import` 在独立 Docker 栈完成 Chromium 与 WebKit 验收，并执行 `down -v`；
3. 共享开发数据库在 E2E 前后不被测试账号、文件或不可变事实污染；
4. Ticket 03 已按批准清单创建本地提交 `0c04926`；
5. 不混入 `.scratch/` 规划、UI 研究、技能安装产物或其他 owner 工作区改动；
6. 不执行 `git push`。

## 当前执行门：Ticket 04

Ticket 04 可以领取，但 To-Agent 文档必须先固定：

1. Graphile Worker 的精确版本、许可证、PostgreSQL 兼容性与 4 GB 资源预算；
2. application operation 记录、Graphile job 与导入行之间的稳定 ID/状态边界；
3. 投递事务、outbox/可恢复投递、at-least-once 消费和业务结果幂等策略；
4. 队列并发、退避、最大尝试和管理员重试权限；
5. fixture handler 与真实供应商 adapter 的接口缝；本票不得联网或实现 Wiktionary/DeepSeek；
6. API、标准后台任务状态页、独立 worker Compose 服务和重启恢复测试。

## 当前工作区纪律

- Ticket 03 与项目治理文档是两个不同提交范围，不得混合提交。
- `.vscode/`、`prototype/brand-color/`、技能安装产物及 owner/Codex 的文档 WIP 继续保持原状。
- 未提交工作不能仅凭报告视为完成；以代码、数据库约束和可复现门禁为准。
- 下一阶段不能因为“代码看起来已写完”而抢跑，必须先越过上一工单的提交门。

## 下一步

1. 按 [`execution-gates.md`](execution-gates.md) 编写工单 04 的完整 To-Agent 文档；
2. 先建立 Worker/operation 深模块和 fixture handler，不提前混入 Wiktionary 或 DeepSeek；
3. 工单 04 通过后，再处理供应商侧决策与适配器。
